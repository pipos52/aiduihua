# NovelChat - 小说文风聊天

一个在 Android 手机端本地训练对话模型的聊天应用。导入 TXT 小说后，应用会学习小说的行文风格与对话模式，并模仿该风格与你对话。

**适配设备**：红米 K40 游戏增强版（天玑 1200，6GB+，Android 11+）

## 核心特性

| 特性 | 说明 |
|------|------|
| 多角色 | 创建多个独立角色，各自训练数据不互通 |
| 多TXT训练 | 每个角色可导入多本 TXT，增量融合到同一模型 |
| 本地训练 | 纯 Markov 链 + 内存表，无需 GPU，秒级完成 |
| 深度对话 | 多候选打分 + 主题召回 + 上下文回溯 + 问答硬记忆 |
| 对话学习 | 每次对话回灌训练，角色逐渐记住你的问法与风格 |
| 持久记忆 | 对话记录与模型自动保存，重启应用不丢失 |

## 架构概览

```
┌─────────────────────────────────────────────┐
│              CharacterListActivity           │
│         (启动入口：角色列表/创建/删除)          │
└──────────────────┬──────────────────────────┘
                   │ 点击角色
                   ▼
┌─────────────────────────────────────────────┐
│                MainActivity                  │
│         (聊天界面：对话/切换角色/清空)           │
└──────────────────┬──────────────────────────┘
                   │
      ┌────────────┼────────────┐
      ▼            ▼            ▼
  MarkovTrainer  TextGenerator  DialogueMemory
  (训练/增量学习)  (生成/打分)    (持久化记忆)
      │            │            │
      └────────────┼────────────┘
                   ▼
              MarkovModel
         (N-gram 转移概率表)
```

## 关键文件

```
app/src/main/java/com/novelchat/app/
├── engine/
│   ├── ChineseTokenizer.kt   # 中文分词器（按字+词混合）
│   ├── MarkovTrainer.kt      # Markov 链训练器（含增量学习）
│   ├── TextGenerator.kt      # 对话生成器（多候选+主题召回+记忆）
│   ├── MarkovModel.kt        # 模型数据结构（N-gram表+主题索引+QA记忆）
│   ├── DialogueMemory.kt     # 对话记忆持久化（JSONL）
│   ├── ModelManager.kt       # 按角色管理模型训练与持久化
│   └── CharacterStore.kt     # 角色列表持久化（JSON）
├── model/
│   ├── Character.kt          # 角色数据类
│   └── ChatMessage.kt        # 聊天消息数据类
├── ui/
│   ├── MainViewModel.kt      # 聊天状态机
│   ├── CharacterListViewModel.kt  # 角色列表状态机
│   ├── ChatAdapter.kt        # 聊天消息列表适配器
│   └── CharacterAdapter.kt   # 角色列表适配器
├── MainActivity.kt           # 聊天界面
└── CharacterListActivity.kt  # 角色管理界面
```

## 深度对话原理

生成回复时采用五层策略叠加：

1. **问答硬记忆**：若曾回答过几乎相同的问题，直接返回历史回复
2. **上下文回溯**：把最近 2 轮用户输入与角色回复的末词作为 Markov 前驱种子
3. **主题召回**：从用户输入提取关键词，从 topicIndex 召回相关原句
4. **多候选打分**：生成 4 个候选回复，按"切题度 + 召回相似度 + 长度合理性 + 多样性"打分择优
5. **风格包装**：按小说对话前后缀模式（如"说道"、"道"），40% 概率用引号包裹

每次对话后自动增量训练：把"问+答"拼接回灌 N-gram 链（答句权重 ×3），同时记录到 qaMemory 和 userVocabulary，让角色逐渐习得你的说话方式。

## 构建方式

### 环境要求

- Android Studio Hedgehog | 2023.1.1+
- JDK 17
- Android SDK 34（compileSdk）
- Gradle 8.2+

### 构建步骤

```bash
# 1. 用 Android Studio 打开项目
#    File -> Open -> 选择 NovelChat 目录

# 2. 等待 Gradle 同步完成（首次需下载依赖）

# 3. 连接手机或选择模拟器，点击 Run
#    或命令行：
./gradlew assembleDebug

# 4. APK 输出位置：
#    app/build/outputs/apk/debug/app-debug.apk
```

### 安装到红米 K40 游戏增强版

```bash
# USB 连接手机，开启 USB 调试
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

## 使用流程

1. **打开应用** → 进入角色列表
2. **点击右下角 +** → 创建角色（填写名称、选择阶数）
3. **点击角色右侧 ⋮** → "继续训练（导入TXT）" → 选择小说文件
4. 等待训练完成（百万字约 5-10 秒）
5. **重复步骤 3** 可继续导入更多小说到同一角色
6. **点击角色名** → 进入聊天 → 开始对话
7. 角色会自动学习你的对话方式，越聊越"懂你"

## 模型阶数选择

| 阶数 | 特点 | 推荐场景 |
|------|------|----------|
| 1 | 最随机，创造力高 | 想要意外惊喜 |
| 2 | 平衡随机与贴合 | 通用场景 |
| 3 | 贴合原文风格（推荐） | 严肃模仿文风 |
| 4 | 最贴合原文，但可能过于死板 | 大量训练文本（>100万字） |
